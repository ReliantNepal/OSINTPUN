# OSINTPUN

Single-file Python OSINT starter focused on legitimate public-data workflows.

## Current module
- Username lookup (manifest-driven, Sherlock-compatible data source inspiration)

## Notes
- Uses public profile existence checks only.
- Sherlock is included under `resources/sherlock` and remains under its original MIT license.
- This tool does not include copied Sherlock source wholesale; it uses a clean single-file implementation inspired by the same manifest-driven approach.

## Run

```bash
python start.py
```
